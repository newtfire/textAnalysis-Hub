<?xml version="1.0" encoding="UTF-8"?>
<svg xmlns="http://www.w3.org/2000/svg"
     width="1000"
     height="1500"
     viewBox="0 0 2000 3000">
   <desc>Cutin Data by Episode</desc>
   <g id="wholePlot" transform="translate(50, 2000)">
      <g id="The Grand Turnabout">
         <text font-size="2em" x="25800" y="50">The Grand Turnabout</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            95</desc>
               <line stroke="red" x1="25801" x2="25800" y1="0" y2="95"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            59</desc>
               <line stroke="green" x1="25802" x2="25800" y1="0" y2="59"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            56</desc>
               <line stroke="blue" x1="25803" x2="25800" y1="0" y2="56"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Turnabout Ablaze">
         <text font-size="2em" x="5400" y="50">Turnabout Ablaze</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            47</desc>
               <line stroke="red" x1="5401" x2="5400" y1="0" y2="47"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            41</desc>
               <line stroke="green" x1="5402" x2="5400" y1="0" y2="41"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            87</desc>
               <line stroke="blue" x1="5403" x2="5400" y1="0" y2="87"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="The Imprisoned Turnabout">
         <text font-size="2em" x="38700" y="50">The Imprisoned Turnabout</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            60</desc>
               <line stroke="red" x1="38701" x2="38700" y1="0" y2="60"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            56</desc>
               <line stroke="green" x1="38702" x2="38700" y1="0" y2="56"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            59</desc>
               <line stroke="blue" x1="38703" x2="38700" y1="0" y2="59"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Bridge to the Turnabout">
         <text font-size="2em" x="42300" y="50">Bridge to the Turnabout</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            60</desc>
               <line stroke="red" x1="42301" x2="42300" y1="0" y2="60"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            50</desc>
               <line stroke="green" x1="42302" x2="42300" y1="0" y2="50"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            57</desc>
               <line stroke="blue" x1="42303" x2="42300" y1="0" y2="57"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Turnabout Ablaze">
         <text font-size="2em" x="900" y="50">Turnabout Ablaze</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            77</desc>
               <line stroke="red" x1="901" x2="900" y1="0" y2="77"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            43</desc>
               <line stroke="green" x1="902" x2="900" y1="0" y2="43"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            42</desc>
               <line stroke="blue" x1="903" x2="900" y1="0" y2="42"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Recipe for Turnabout">
         <text font-size="2em" x="29700" y="50">Recipe for Turnabout</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            60</desc>
               <line stroke="red" x1="29701" x2="29700" y1="0" y2="60"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            44</desc>
               <line stroke="green" x1="29702" x2="29700" y1="0" y2="44"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            50</desc>
               <line stroke="blue" x1="29703" x2="29700" y1="0" y2="50"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Farewell, My Turnabout">
         <text font-size="2em" x="12900" y="50">Farewell, My Turnabout</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            54</desc>
               <line stroke="red" x1="12901" x2="12900" y1="0" y2="54"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            28</desc>
               <line stroke="green" x1="12902" x2="12900" y1="0" y2="28"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            62</desc>
               <line stroke="blue" x1="12903" x2="12900" y1="0" y2="62"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Rise from the Ashes">
         <text font-size="2em" x="15000" y="50">Rise from the Ashes</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            62</desc>
               <line stroke="red" x1="15001" x2="15000" y1="0" y2="62"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            38</desc>
               <line stroke="green" x1="15002" x2="15000" y1="0" y2="38"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            39</desc>
               <line stroke="blue" x1="15003" x2="15000" y1="0" y2="39"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Turnabout Beginnings">
         <text font-size="2em" x="40500" y="50">Turnabout Beginnings</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            67</desc>
               <line stroke="red" x1="40501" x2="40500" y1="0" y2="67"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            25</desc>
               <line stroke="green" x1="40502" x2="40500" y1="0" y2="25"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            45</desc>
               <line stroke="blue" x1="40503" x2="40500" y1="0" y2="45"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Turnabout Trump">
         <text font-size="2em" x="48300" y="50">Turnabout Trump</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            54</desc>
               <line stroke="red" x1="48301" x2="48300" y1="0" y2="54"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            32</desc>
               <line stroke="green" x1="48302" x2="48300" y1="0" y2="32"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            46</desc>
               <line stroke="blue" x1="48303" x2="48300" y1="0" y2="46"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="The Kidnapped Turnabout">
         <text font-size="2em" x="16800" y="50">The Kidnapped Turnabout</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            22</desc>
               <line stroke="red" x1="16801" x2="16800" y1="0" y2="22"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            66</desc>
               <line stroke="green" x1="16802" x2="16800" y1="0" y2="66"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            36</desc>
               <line stroke="blue" x1="16803" x2="16800" y1="0" y2="36"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="The Stolen Turnabout">
         <text font-size="2em" x="36300" y="50">The Stolen Turnabout</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            52</desc>
               <line stroke="red" x1="36301" x2="36300" y1="0" y2="52"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            27</desc>
               <line stroke="green" x1="36302" x2="36300" y1="0" y2="27"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            45</desc>
               <line stroke="blue" x1="36303" x2="36300" y1="0" y2="45"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="The Forgotten Turnabout">
         <text font-size="2em" x="46500" y="50">The Forgotten Turnabout</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            53</desc>
               <line stroke="red" x1="46501" x2="46500" y1="0" y2="53"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            25</desc>
               <line stroke="green" x1="46502" x2="46500" y1="0" y2="25"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            45</desc>
               <line stroke="blue" x1="46503" x2="46500" y1="0" y2="45"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="The Adventure of the Unspeakable Story">
         <text font-size="2em" x="11100" y="50">The Adventure of the Unspeakable Story</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            36</desc>
               <line stroke="red" x1="11101" x2="11100" y1="0" y2="36"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            0</desc>
               <line stroke="green" x1="11102" x2="11100" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            86</desc>
               <line stroke="blue" x1="11103" x2="11100" y1="0" y2="86"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="The Inherited Turnabout">
         <text font-size="2em" x="21300" y="50">The Inherited Turnabout</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            48</desc>
               <line stroke="red" x1="21301" x2="21300" y1="0" y2="48"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            31</desc>
               <line stroke="green" x1="21302" x2="21300" y1="0" y2="31"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            38</desc>
               <line stroke="blue" x1="21303" x2="21300" y1="0" y2="38"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Turnabout Memories">
         <text font-size="2em" x="8700" y="50">Turnabout Memories</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            51</desc>
               <line stroke="red" x1="8701" x2="8700" y1="0" y2="51"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            17</desc>
               <line stroke="green" x1="8702" x2="8700" y1="0" y2="17"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            47</desc>
               <line stroke="blue" x1="8703" x2="8700" y1="0" y2="47"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Turnabout Target">
         <text font-size="2em" x="36000" y="50">Turnabout Target</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            38</desc>
               <line stroke="red" x1="36001" x2="36000" y1="0" y2="38"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            36</desc>
               <line stroke="green" x1="36002" x2="36000" y1="0" y2="36"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            38</desc>
               <line stroke="blue" x1="36003" x2="36000" y1="0" y2="38"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Rise from the Ashes">
         <text font-size="2em" x="11700" y="50">Rise from the Ashes</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            46</desc>
               <line stroke="red" x1="11701" x2="11700" y1="0" y2="46"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            34</desc>
               <line stroke="green" x1="11702" x2="11700" y1="0" y2="34"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            31</desc>
               <line stroke="blue" x1="11703" x2="11700" y1="0" y2="31"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Turnabout Serenade">
         <text font-size="2em" x="38400" y="50">Turnabout Serenade</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            36</desc>
               <line stroke="red" x1="38401" x2="38400" y1="0" y2="36"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            29</desc>
               <line stroke="green" x1="38402" x2="38400" y1="0" y2="29"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            44</desc>
               <line stroke="blue" x1="38403" x2="38400" y1="0" y2="44"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Farewell, My Turnabout">
         <text font-size="2em" x="4800" y="50">Farewell, My Turnabout</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            34</desc>
               <line stroke="red" x1="4801" x2="4800" y1="0" y2="34"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            17</desc>
               <line stroke="green" x1="4802" x2="4800" y1="0" y2="17"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            51</desc>
               <line stroke="blue" x1="4803" x2="4800" y1="0" y2="51"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="The Adventure of the Clouded Kokoro">
         <text font-size="2em" x="17400" y="50">The Adventure of the Clouded Kokoro</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            40</desc>
               <line stroke="red" x1="17401" x2="17400" y1="0" y2="40"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            0</desc>
               <line stroke="green" x1="17402" x2="17400" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            54</desc>
               <line stroke="blue" x1="17403" x2="17400" y1="0" y2="54"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Turnabout Corner">
         <text font-size="2em" x="48900" y="50">Turnabout Corner</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            38</desc>
               <line stroke="red" x1="48901" x2="48900" y1="0" y2="38"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            30</desc>
               <line stroke="green" x1="48902" x2="48900" y1="0" y2="30"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            23</desc>
               <line stroke="blue" x1="48903" x2="48900" y1="0" y2="23"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Turnabout Reminiscence">
         <text font-size="2em" x="50700" y="50">Turnabout Reminiscence</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            36</desc>
               <line stroke="red" x1="50701" x2="50700" y1="0" y2="36"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            12</desc>
               <line stroke="green" x1="50702" x2="50700" y1="0" y2="12"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            40</desc>
               <line stroke="blue" x1="50703" x2="50700" y1="0" y2="40"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Turnabout Airlines">
         <text font-size="2em" x="6300" y="50">Turnabout Airlines</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            24</desc>
               <line stroke="red" x1="6301" x2="6300" y1="0" y2="24"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            29</desc>
               <line stroke="green" x1="6302" x2="6300" y1="0" y2="29"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            34</desc>
               <line stroke="blue" x1="6303" x2="6300" y1="0" y2="34"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="The Resolve of Ryunosuke Naruhodo">
         <text font-size="2em" x="15900" y="50">The Resolve of Ryunosuke Naruhodo</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            47</desc>
               <line stroke="red" x1="15901" x2="15900" y1="0" y2="47"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            0</desc>
               <line stroke="green" x1="15902" x2="15900" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            40</desc>
               <line stroke="blue" x1="15903" x2="15900" y1="0" y2="40"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="The Lost Turnabout">
         <text font-size="2em" x="46800" y="50">The Lost Turnabout</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            42</desc>
               <line stroke="red" x1="46801" x2="46800" y1="0" y2="42"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            13</desc>
               <line stroke="green" x1="46802" x2="46800" y1="0" y2="13"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            31</desc>
               <line stroke="blue" x1="46803" x2="46800" y1="0" y2="31"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Rise from the Ashes">
         <text font-size="2em" x="4500" y="50">Rise from the Ashes</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            40</desc>
               <line stroke="red" x1="4501" x2="4500" y1="0" y2="40"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            13</desc>
               <line stroke="green" x1="4502" x2="4500" y1="0" y2="13"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            32</desc>
               <line stroke="blue" x1="4503" x2="4500" y1="0" y2="32"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="The Adventure of the Runaway Room">
         <text font-size="2em" x="9000" y="50">The Adventure of the Runaway Room</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            26</desc>
               <line stroke="red" x1="9001" x2="9000" y1="0" y2="26"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            0</desc>
               <line stroke="green" x1="9002" x2="9000" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            58</desc>
               <line stroke="blue" x1="9003" x2="9000" y1="0" y2="58"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Bridge to the Turnabout">
         <text font-size="2em" x="49200" y="50">Bridge to the Turnabout</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            22</desc>
               <line stroke="red" x1="49201" x2="49200" y1="0" y2="22"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            11</desc>
               <line stroke="green" x1="49202" x2="49200" y1="0" y2="11"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            49</desc>
               <line stroke="blue" x1="49203" x2="49200" y1="0" y2="49"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Turnabout Sisters">
         <text font-size="2em" x="30900" y="50">Turnabout Sisters</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            32</desc>
               <line stroke="red" x1="30901" x2="30900" y1="0" y2="32"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            18</desc>
               <line stroke="green" x1="30902" x2="30900" y1="0" y2="18"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            27</desc>
               <line stroke="blue" x1="30903" x2="30900" y1="0" y2="27"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="The Imprisoned Turnabout">
         <text font-size="2em" x="35400" y="50">The Imprisoned Turnabout</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            24</desc>
               <line stroke="red" x1="35401" x2="35400" y1="0" y2="24"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            14</desc>
               <line stroke="green" x1="35402" x2="35400" y1="0" y2="14"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            39</desc>
               <line stroke="blue" x1="35403" x2="35400" y1="0" y2="39"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Turnabout Goodbyes">
         <text font-size="2em" x="30000" y="50">Turnabout Goodbyes</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            38</desc>
               <line stroke="red" x1="30001" x2="30000" y1="0" y2="38"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            10</desc>
               <line stroke="green" x1="30002" x2="30000" y1="0" y2="10"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            27</desc>
               <line stroke="blue" x1="30003" x2="30000" y1="0" y2="27"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="The Adventure of the Blossoming Attorney">
         <text font-size="2em" x="51000" y="50">The Adventure of the Blossoming Attorney</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            37</desc>
               <line stroke="red" x1="51001" x2="51000" y1="0" y2="37"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            0</desc>
               <line stroke="green" x1="51002" x2="51000" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            38</desc>
               <line stroke="blue" x1="51003" x2="51000" y1="0" y2="38"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Twisted Karma and His Last Bow">
         <text font-size="2em" x="24900" y="50">Twisted Karma and His Last Bow</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            34</desc>
               <line stroke="red" x1="24901" x2="24900" y1="0" y2="34"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            0</desc>
               <line stroke="green" x1="24902" x2="24900" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            40</desc>
               <line stroke="blue" x1="24903" x2="24900" y1="0" y2="40"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Turnabout Succession">
         <text font-size="2em" x="50400" y="50">Turnabout Succession</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            31</desc>
               <line stroke="red" x1="50401" x2="50400" y1="0" y2="31"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            11</desc>
               <line stroke="green" x1="50402" x2="50400" y1="0" y2="11"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            31</desc>
               <line stroke="blue" x1="50403" x2="50400" y1="0" y2="31"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="The Return of the Great Departed Soul">
         <text font-size="2em" x="23700" y="50">The Return of the Great Departed Soul</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            32</desc>
               <line stroke="red" x1="23701" x2="23700" y1="0" y2="32"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            0</desc>
               <line stroke="green" x1="23702" x2="23700" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            39</desc>
               <line stroke="blue" x1="23703" x2="23700" y1="0" y2="39"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Turnabout Goodbyes">
         <text font-size="2em" x="37200" y="50">Turnabout Goodbyes</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            36</desc>
               <line stroke="red" x1="37201" x2="37200" y1="0" y2="36"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            20</desc>
               <line stroke="green" x1="37202" x2="37200" y1="0" y2="20"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            15</desc>
               <line stroke="blue" x1="37203" x2="37200" y1="0" y2="15"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Turnabout Sisters">
         <text font-size="2em" x="37500" y="50">Turnabout Sisters</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            33</desc>
               <line stroke="red" x1="37501" x2="37500" y1="0" y2="33"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            7</desc>
               <line stroke="green" x1="37502" x2="37500" y1="0" y2="7"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            31</desc>
               <line stroke="blue" x1="37503" x2="37500" y1="0" y2="31"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Turnabout Big Top">
         <text font-size="2em" x="19200" y="50">Turnabout Big Top</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            17</desc>
               <line stroke="red" x1="19201" x2="19200" y1="0" y2="17"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            10</desc>
               <line stroke="green" x1="19202" x2="19200" y1="0" y2="10"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            41</desc>
               <line stroke="blue" x1="19203" x2="19200" y1="0" y2="41"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Turnabout Succession">
         <text font-size="2em" x="51900" y="50">Turnabout Succession</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            34</desc>
               <line stroke="red" x1="51901" x2="51900" y1="0" y2="34"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            10</desc>
               <line stroke="green" x1="51902" x2="51900" y1="0" y2="10"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            24</desc>
               <line stroke="blue" x1="51903" x2="51900" y1="0" y2="24"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="The Adventure of the Great Departure">
         <text font-size="2em" x="20100" y="50">The Adventure of the Great Departure</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            61</desc>
               <line stroke="red" x1="20101" x2="20100" y1="0" y2="61"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            0</desc>
               <line stroke="green" x1="20102" x2="20100" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            6</desc>
               <line stroke="blue" x1="20103" x2="20100" y1="0" y2="6"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Turnabout Visitor">
         <text font-size="2em" x="39900" y="50">Turnabout Visitor</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            23</desc>
               <line stroke="red" x1="39901" x2="39900" y1="0" y2="23"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            14</desc>
               <line stroke="green" x1="39902" x2="39900" y1="0" y2="14"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            29</desc>
               <line stroke="blue" x1="39903" x2="39900" y1="0" y2="29"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Reunion, and Turnabout">
         <text font-size="2em" x="42900" y="50">Reunion, and Turnabout</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            12</desc>
               <line stroke="red" x1="42901" x2="42900" y1="0" y2="12"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            22</desc>
               <line stroke="green" x1="42902" x2="42900" y1="0" y2="22"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            32</desc>
               <line stroke="blue" x1="42903" x2="42900" y1="0" y2="32"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Turnabout Big Top">
         <text font-size="2em" x="24600" y="50">Turnabout Big Top</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            13</desc>
               <line stroke="red" x1="24601" x2="24600" y1="0" y2="13"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            32</desc>
               <line stroke="green" x1="24602" x2="24600" y1="0" y2="32"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            16</desc>
               <line stroke="blue" x1="24603" x2="24600" y1="0" y2="16"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="The Inherited Turnabout">
         <text font-size="2em" x="18900" y="50">The Inherited Turnabout</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            26</desc>
               <line stroke="red" x1="18901" x2="18900" y1="0" y2="26"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            18</desc>
               <line stroke="green" x1="18902" x2="18900" y1="0" y2="18"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            16</desc>
               <line stroke="blue" x1="18903" x2="18900" y1="0" y2="16"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Turnabout Goodbyes">
         <text font-size="2em" x="36600" y="50">Turnabout Goodbyes</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            31</desc>
               <line stroke="red" x1="36601" x2="36600" y1="0" y2="31"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            10</desc>
               <line stroke="green" x1="36602" x2="36600" y1="0" y2="10"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            19</desc>
               <line stroke="blue" x1="36603" x2="36600" y1="0" y2="19"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="The Kidnapped Turnabout">
         <text font-size="2em" x="12600" y="50">The Kidnapped Turnabout</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            18</desc>
               <line stroke="red" x1="12601" x2="12600" y1="0" y2="18"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            10</desc>
               <line stroke="green" x1="12602" x2="12600" y1="0" y2="10"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            30</desc>
               <line stroke="blue" x1="12603" x2="12600" y1="0" y2="30"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="The Grand Turnabout">
         <text font-size="2em" x="23100" y="50">The Grand Turnabout</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            33</desc>
               <line stroke="red" x1="23101" x2="23100" y1="0" y2="33"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            14</desc>
               <line stroke="green" x1="23102" x2="23100" y1="0" y2="14"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            11</desc>
               <line stroke="blue" x1="23103" x2="23100" y1="0" y2="11"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Recipe for Turnabout">
         <text font-size="2em" x="35700" y="50">Recipe for Turnabout</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            25</desc>
               <line stroke="red" x1="35701" x2="35700" y1="0" y2="25"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            0</desc>
               <line stroke="green" x1="35702" x2="35700" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            33</desc>
               <line stroke="blue" x1="35703" x2="35700" y1="0" y2="33"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Turnabout Corner">
         <text font-size="2em" x="41400" y="50">Turnabout Corner</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            13</desc>
               <line stroke="red" x1="41401" x2="41400" y1="0" y2="13"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            12</desc>
               <line stroke="green" x1="41402" x2="41400" y1="0" y2="12"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            32</desc>
               <line stroke="blue" x1="41403" x2="41400" y1="0" y2="32"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Turnabout Samurai">
         <text font-size="2em" x="13800" y="50">Turnabout Samurai</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            16</desc>
               <line stroke="red" x1="13801" x2="13800" y1="0" y2="16"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            9</desc>
               <line stroke="green" x1="13802" x2="13800" y1="0" y2="9"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            31</desc>
               <line stroke="blue" x1="13803" x2="13800" y1="0" y2="31"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Turnabout Serenade">
         <text font-size="2em" x="31800" y="50">Turnabout Serenade</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            14</desc>
               <line stroke="red" x1="31801" x2="31800" y1="0" y2="14"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            11</desc>
               <line stroke="green" x1="31802" x2="31800" y1="0" y2="11"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            31</desc>
               <line stroke="blue" x1="31803" x2="31800" y1="0" y2="31"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="The Stolen Turnabout">
         <text font-size="2em" x="30300" y="50">The Stolen Turnabout</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            18</desc>
               <line stroke="red" x1="30301" x2="30300" y1="0" y2="18"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            13</desc>
               <line stroke="green" x1="30302" x2="30300" y1="0" y2="13"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            23</desc>
               <line stroke="blue" x1="30303" x2="30300" y1="0" y2="23"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Reunion, and Turnabout">
         <text font-size="2em" x="50100" y="50">Reunion, and Turnabout</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            12</desc>
               <line stroke="red" x1="50101" x2="50100" y1="0" y2="12"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            14</desc>
               <line stroke="green" x1="50102" x2="50100" y1="0" y2="14"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            26</desc>
               <line stroke="blue" x1="50103" x2="50100" y1="0" y2="26"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Turnabout Reminiscence">
         <text font-size="2em" x="47400" y="50">Turnabout Reminiscence</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            21</desc>
               <line stroke="red" x1="47401" x2="47400" y1="0" y2="21"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            10</desc>
               <line stroke="green" x1="47402" x2="47400" y1="0" y2="10"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            20</desc>
               <line stroke="blue" x1="47403" x2="47400" y1="0" y2="20"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Turnabout Airlines">
         <text font-size="2em" x="3300" y="50">Turnabout Airlines</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            18</desc>
               <line stroke="red" x1="3301" x2="3300" y1="0" y2="18"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            17</desc>
               <line stroke="green" x1="3302" x2="3300" y1="0" y2="17"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            15</desc>
               <line stroke="blue" x1="3303" x2="3300" y1="0" y2="15"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="The Memoirs of the Clouded Kokoro">
         <text font-size="2em" x="10800" y="50">The Memoirs of the Clouded Kokoro</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            16</desc>
               <line stroke="red" x1="10801" x2="10800" y1="0" y2="16"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            0</desc>
               <line stroke="green" x1="10802" x2="10800" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            32</desc>
               <line stroke="blue" x1="10803" x2="10800" y1="0" y2="32"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Turnabout Succession">
         <text font-size="2em" x="45000" y="50">Turnabout Succession</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            26</desc>
               <line stroke="red" x1="45001" x2="45000" y1="0" y2="26"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            14</desc>
               <line stroke="green" x1="45002" x2="45000" y1="0" y2="14"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            6</desc>
               <line stroke="blue" x1="45003" x2="45000" y1="0" y2="6"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Turnabout Samurai">
         <text font-size="2em" x="5700" y="50">Turnabout Samurai</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            14</desc>
               <line stroke="red" x1="5701" x2="5700" y1="0" y2="14"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            3</desc>
               <line stroke="green" x1="5702" x2="5700" y1="0" y2="3"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            28</desc>
               <line stroke="blue" x1="5703" x2="5700" y1="0" y2="28"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="The Return of the Great Departed Soul">
         <text font-size="2em" x="18000" y="50">The Return of the Great Departed Soul</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            17</desc>
               <line stroke="red" x1="18001" x2="18000" y1="0" y2="17"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            0</desc>
               <line stroke="green" x1="18002" x2="18000" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            27</desc>
               <line stroke="blue" x1="18003" x2="18000" y1="0" y2="27"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="The Memoirs of the Clouded Kokoro">
         <text font-size="2em" x="1800" y="50">The Memoirs of the Clouded Kokoro</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            14</desc>
               <line stroke="red" x1="1801" x2="1800" y1="0" y2="14"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            0</desc>
               <line stroke="green" x1="1802" x2="1800" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            29</desc>
               <line stroke="blue" x1="1803" x2="1800" y1="0" y2="29"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="The First Turnabout">
         <text font-size="2em" x="26700" y="50">The First Turnabout</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            16</desc>
               <line stroke="red" x1="26701" x2="26700" y1="0" y2="16"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            4</desc>
               <line stroke="green" x1="26702" x2="26700" y1="0" y2="4"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            20</desc>
               <line stroke="blue" x1="26703" x2="26700" y1="0" y2="20"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Turnabout Samurai">
         <text font-size="2em" x="12000" y="50">Turnabout Samurai</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            14</desc>
               <line stroke="red" x1="12001" x2="12000" y1="0" y2="14"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            11</desc>
               <line stroke="green" x1="12002" x2="12000" y1="0" y2="11"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            14</desc>
               <line stroke="blue" x1="12003" x2="12000" y1="0" y2="14"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="The Forgotten Turnabout">
         <text font-size="2em" x="49800" y="50">The Forgotten Turnabout</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            19</desc>
               <line stroke="red" x1="49801" x2="49800" y1="0" y2="19"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            2</desc>
               <line stroke="green" x1="49802" x2="49800" y1="0" y2="2"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            17</desc>
               <line stroke="blue" x1="49803" x2="49800" y1="0" y2="17"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Recipe for Turnabout">
         <text font-size="2em" x="35100" y="50">Recipe for Turnabout</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            1</desc>
               <line stroke="red" x1="35101" x2="35100" y1="0" y2="1"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            28</desc>
               <line stroke="green" x1="35102" x2="35100" y1="0" y2="28"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            1</desc>
               <line stroke="blue" x1="35103" x2="35100" y1="0" y2="1"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Turnabout Succession">
         <text font-size="2em" x="43500" y="50">Turnabout Succession</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            1</desc>
               <line stroke="red" x1="43501" x2="43500" y1="0" y2="1"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            29</desc>
               <line stroke="green" x1="43502" x2="43500" y1="0" y2="29"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            0</desc>
               <line stroke="blue" x1="43503" x2="43500" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Reunion, and Turnabout">
         <text font-size="2em" x="52500" y="50">Reunion, and Turnabout</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            0</desc>
               <line stroke="red" x1="52501" x2="52500" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            30</desc>
               <line stroke="green" x1="52502" x2="52500" y1="0" y2="30"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            0</desc>
               <line stroke="blue" x1="52503" x2="52500" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Ryunosuke Naruhodo's Seven Days of Sin">
         <text font-size="2em" x="40200" y="50">Ryunosuke Naruhodo's Seven Days of Sin</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            21</desc>
               <line stroke="red" x1="40201" x2="40200" y1="0" y2="21"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            0</desc>
               <line stroke="green" x1="40202" x2="40200" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            8</desc>
               <line stroke="blue" x1="40203" x2="40200" y1="0" y2="8"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="The Resolve of Ryunosuke Naruhodo">
         <text font-size="2em" x="10500" y="50">The Resolve of Ryunosuke Naruhodo</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            14</desc>
               <line stroke="red" x1="10501" x2="10500" y1="0" y2="14"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            0</desc>
               <line stroke="green" x1="10502" x2="10500" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            13</desc>
               <line stroke="blue" x1="10503" x2="10500" y1="0" y2="13"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Bridge to the Turnabout">
         <text font-size="2em" x="45900" y="50">Bridge to the Turnabout</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            0</desc>
               <line stroke="red" x1="45901" x2="45900" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            24</desc>
               <line stroke="green" x1="45902" x2="45900" y1="0" y2="24"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            2</desc>
               <line stroke="blue" x1="45903" x2="45900" y1="0" y2="2"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Farewell, My Turnabout">
         <text font-size="2em" x="9600" y="50">Farewell, My Turnabout</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            0</desc>
               <line stroke="red" x1="9601" x2="9600" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            21</desc>
               <line stroke="green" x1="9602" x2="9600" y1="0" y2="21"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            0</desc>
               <line stroke="blue" x1="9603" x2="9600" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Recipe for Turnabout">
         <text font-size="2em" x="39300" y="50">Recipe for Turnabout</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            0</desc>
               <line stroke="red" x1="39301" x2="39300" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            21</desc>
               <line stroke="green" x1="39302" x2="39300" y1="0" y2="21"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            0</desc>
               <line stroke="blue" x1="39303" x2="39300" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="The Stolen Turnabout">
         <text font-size="2em" x="27300" y="50">The Stolen Turnabout</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            0</desc>
               <line stroke="red" x1="27301" x2="27300" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            18</desc>
               <line stroke="green" x1="27302" x2="27300" y1="0" y2="18"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            0</desc>
               <line stroke="blue" x1="27303" x2="27300" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Farewell, My Turnabout">
         <text font-size="2em" x="17100" y="50">Farewell, My Turnabout</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            0</desc>
               <line stroke="red" x1="17101" x2="17100" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            17</desc>
               <line stroke="green" x1="17102" x2="17100" y1="0" y2="17"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            0</desc>
               <line stroke="blue" x1="17103" x2="17100" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Bridge to the Turnabout">
         <text font-size="2em" x="52800" y="50">Bridge to the Turnabout</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            2</desc>
               <line stroke="red" x1="52801" x2="52800" y1="0" y2="2"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            14</desc>
               <line stroke="green" x1="52802" x2="52800" y1="0" y2="14"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            1</desc>
               <line stroke="blue" x1="52803" x2="52800" y1="0" y2="1"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Turnabout Big Top">
         <text font-size="2em" x="18300" y="50">Turnabout Big Top</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            0</desc>
               <line stroke="red" x1="18301" x2="18300" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            14</desc>
               <line stroke="green" x1="18302" x2="18300" y1="0" y2="14"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            0</desc>
               <line stroke="blue" x1="18303" x2="18300" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Turnabout Big Top">
         <text font-size="2em" x="21900" y="50">Turnabout Big Top</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            0</desc>
               <line stroke="red" x1="21901" x2="21900" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            10</desc>
               <line stroke="green" x1="21902" x2="21900" y1="0" y2="10"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            1</desc>
               <line stroke="blue" x1="21903" x2="21900" y1="0" y2="1"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Turnabout Corner">
         <text font-size="2em" x="40800" y="50">Turnabout Corner</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            1</desc>
               <line stroke="red" x1="40801" x2="40800" y1="0" y2="1"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            10</desc>
               <line stroke="green" x1="40802" x2="40800" y1="0" y2="10"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            0</desc>
               <line stroke="blue" x1="40803" x2="40800" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Turnabout Samurai">
         <text font-size="2em" x="16200" y="50">Turnabout Samurai</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            0</desc>
               <line stroke="red" x1="16201" x2="16200" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            9</desc>
               <line stroke="green" x1="16202" x2="16200" y1="0" y2="9"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            0</desc>
               <line stroke="blue" x1="16203" x2="16200" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Special Trial 2017">
         <text font-size="2em" x="24300" y="50">Special Trial 2017</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            3</desc>
               <line stroke="red" x1="24301" x2="24300" y1="0" y2="3"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            0</desc>
               <line stroke="green" x1="24302" x2="24300" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            3</desc>
               <line stroke="blue" x1="24303" x2="24300" y1="0" y2="3"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="In the Bailey">
         <text font-size="2em" x="34500" y="50">In the Bailey</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            4</desc>
               <line stroke="red" x1="34501" x2="34500" y1="0" y2="4"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            0</desc>
               <line stroke="green" x1="34502" x2="34500" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            2</desc>
               <line stroke="blue" x1="34503" x2="34500" y1="0" y2="2"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Rise from the Ashes">
         <text font-size="2em" x="10200" y="50">Rise from the Ashes</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            1</desc>
               <line stroke="red" x1="10201" x2="10200" y1="0" y2="1"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            4</desc>
               <line stroke="green" x1="10202" x2="10200" y1="0" y2="4"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            0</desc>
               <line stroke="blue" x1="10203" x2="10200" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="The Stolen Turnabout">
         <text font-size="2em" x="33000" y="50">The Stolen Turnabout</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            0</desc>
               <line stroke="red" x1="33001" x2="33000" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            5</desc>
               <line stroke="green" x1="33002" x2="33000" y1="0" y2="5"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            0</desc>
               <line stroke="blue" x1="33003" x2="33000" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="The Cosmic Turnabout">
         <text font-size="2em" x="17700" y="50">The Cosmic Turnabout</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            3</desc>
               <line stroke="red" x1="17701" x2="17700" y1="0" y2="3"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            0</desc>
               <line stroke="green" x1="17702" x2="17700" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            1</desc>
               <line stroke="blue" x1="17703" x2="17700" y1="0" y2="1"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="The Return of the Great Departed Soul">
         <text font-size="2em" x="21000" y="50">The Return of the Great Departed Soul</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            3</desc>
               <line stroke="red" x1="21001" x2="21000" y1="0" y2="3"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            0</desc>
               <line stroke="green" x1="21002" x2="21000" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            1</desc>
               <line stroke="blue" x1="21003" x2="21000" y1="0" y2="1"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="The Golden Court">
         <text font-size="2em" x="22800" y="50">The Golden Court</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            0</desc>
               <line stroke="red" x1="22801" x2="22800" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            0</desc>
               <line stroke="green" x1="22802" x2="22800" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            4</desc>
               <line stroke="blue" x1="22803" x2="22800" y1="0" y2="4"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Turnabout Goodbyes">
         <text font-size="2em" x="27600" y="50">Turnabout Goodbyes</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            0</desc>
               <line stroke="red" x1="27601" x2="27600" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            4</desc>
               <line stroke="green" x1="27602" x2="27600" y1="0" y2="4"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            0</desc>
               <line stroke="blue" x1="27603" x2="27600" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Turnabout Serenade">
         <text font-size="2em" x="28500" y="50">Turnabout Serenade</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            0</desc>
               <line stroke="red" x1="28501" x2="28500" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            4</desc>
               <line stroke="green" x1="28502" x2="28500" y1="0" y2="4"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            0</desc>
               <line stroke="blue" x1="28503" x2="28500" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Turnabout Corner">
         <text font-size="2em" x="44700" y="50">Turnabout Corner</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            0</desc>
               <line stroke="red" x1="44701" x2="44700" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            4</desc>
               <line stroke="green" x1="44702" x2="44700" y1="0" y2="4"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            0</desc>
               <line stroke="blue" x1="44703" x2="44700" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="The Adventure of the Unbreakable Speckled Band">
         <text font-size="2em" x="12300" y="50">The Adventure of the Unbreakable Speckled Band</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            0</desc>
               <line stroke="red" x1="12301" x2="12300" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            0</desc>
               <line stroke="green" x1="12302" x2="12300" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            3</desc>
               <line stroke="blue" x1="12303" x2="12300" y1="0" y2="3"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Twisted Karma and His Last Bow">
         <text font-size="2em" x="22500" y="50">Twisted Karma and His Last Bow</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            3</desc>
               <line stroke="red" x1="22501" x2="22500" y1="0" y2="3"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            0</desc>
               <line stroke="green" x1="22502" x2="22500" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            0</desc>
               <line stroke="blue" x1="22503" x2="22500" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Turnabout Serenade">
         <text font-size="2em" x="32400" y="50">Turnabout Serenade</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            1</desc>
               <line stroke="red" x1="32401" x2="32400" y1="0" y2="1"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            2</desc>
               <line stroke="green" x1="32402" x2="32400" y1="0" y2="2"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            0</desc>
               <line stroke="blue" x1="32403" x2="32400" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Turnabout Revolution">
         <text font-size="2em" x="37800" y="50">Turnabout Revolution</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            0</desc>
               <line stroke="red" x1="37801" x2="37800" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            0</desc>
               <line stroke="green" x1="37802" x2="37800" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            3</desc>
               <line stroke="blue" x1="37803" x2="37800" y1="0" y2="3"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Reunion, and Turnabout">
         <text font-size="2em" x="46200" y="50">Reunion, and Turnabout</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            0</desc>
               <line stroke="red" x1="46201" x2="46200" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            3</desc>
               <line stroke="green" x1="46202" x2="46200" y1="0" y2="3"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            0</desc>
               <line stroke="blue" x1="46203" x2="46200" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="The Magical Turnabout">
         <text font-size="2em" x="3900" y="50">The Magical Turnabout</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            2</desc>
               <line stroke="red" x1="3901" x2="3900" y1="0" y2="2"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            0</desc>
               <line stroke="green" x1="3902" x2="3900" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            0</desc>
               <line stroke="blue" x1="3903" x2="3900" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="The Magical Turnabout">
         <text font-size="2em" x="6600" y="50">The Magical Turnabout</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            2</desc>
               <line stroke="red" x1="6601" x2="6600" y1="0" y2="2"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            0</desc>
               <line stroke="green" x1="6602" x2="6600" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            0</desc>
               <line stroke="blue" x1="6603" x2="6600" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Turnabout Academy">
         <text font-size="2em" x="6900" y="50">Turnabout Academy</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            1</desc>
               <line stroke="red" x1="6901" x2="6900" y1="0" y2="1"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            0</desc>
               <line stroke="green" x1="6902" x2="6900" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            1</desc>
               <line stroke="blue" x1="6903" x2="6900" y1="0" y2="1"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Turnabout Academy">
         <text font-size="2em" x="11400" y="50">Turnabout Academy</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            2</desc>
               <line stroke="red" x1="11401" x2="11400" y1="0" y2="2"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            0</desc>
               <line stroke="green" x1="11402" x2="11400" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            0</desc>
               <line stroke="blue" x1="11403" x2="11400" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="The Adventure of the Unspeakable Story">
         <text font-size="2em" x="15300" y="50">The Adventure of the Unspeakable Story</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            0</desc>
               <line stroke="red" x1="15301" x2="15300" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            0</desc>
               <line stroke="green" x1="15302" x2="15300" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            2</desc>
               <line stroke="blue" x1="15303" x2="15300" y1="0" y2="2"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="The Foreign Turnabout">
         <text font-size="2em" x="47700" y="50">The Foreign Turnabout</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            0</desc>
               <line stroke="red" x1="47701" x2="47700" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            0</desc>
               <line stroke="green" x1="47702" x2="47700" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            2</desc>
               <line stroke="blue" x1="47703" x2="47700" y1="0" y2="2"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="The Monstrous Turnabout">
         <text font-size="2em" x="48000" y="50">The Monstrous Turnabout</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            2</desc>
               <line stroke="red" x1="48001" x2="48000" y1="0" y2="2"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            0</desc>
               <line stroke="green" x1="48002" x2="48000" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            0</desc>
               <line stroke="blue" x1="48003" x2="48000" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Turnabout Countdown">
         <text font-size="2em" x="300" y="50">Turnabout Countdown</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            1</desc>
               <line stroke="red" x1="301" x2="300" y1="0" y2="1"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            0</desc>
               <line stroke="green" x1="302" x2="300" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            0</desc>
               <line stroke="blue" x1="303" x2="300" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Turnabout Time Traveler">
         <text font-size="2em" x="1200" y="50">Turnabout Time Traveler</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            0</desc>
               <line stroke="red" x1="1201" x2="1200" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            0</desc>
               <line stroke="green" x1="1202" x2="1200" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            1</desc>
               <line stroke="blue" x1="1203" x2="1200" y1="0" y2="1"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Turnabout Academy">
         <text font-size="2em" x="3600" y="50">Turnabout Academy</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            1</desc>
               <line stroke="red" x1="3601" x2="3600" y1="0" y2="1"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            0</desc>
               <line stroke="green" x1="3602" x2="3600" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            0</desc>
               <line stroke="blue" x1="3603" x2="3600" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Turnabout Time Traveler">
         <text font-size="2em" x="5100" y="50">Turnabout Time Traveler</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            1</desc>
               <line stroke="red" x1="5101" x2="5100" y1="0" y2="1"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            0</desc>
               <line stroke="green" x1="5102" x2="5100" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            0</desc>
               <line stroke="blue" x1="5103" x2="5100" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Turnabout Time Traveler">
         <text font-size="2em" x="14100" y="50">Turnabout Time Traveler</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            1</desc>
               <line stroke="red" x1="14101" x2="14100" y1="0" y2="1"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            0</desc>
               <line stroke="green" x1="14102" x2="14100" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            0</desc>
               <line stroke="blue" x1="14103" x2="14100" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="The Return of the Great Departed Soul">
         <text font-size="2em" x="19500" y="50">The Return of the Great Departed Soul</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            1</desc>
               <line stroke="red" x1="19501" x2="19500" y1="0" y2="1"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            0</desc>
               <line stroke="green" x1="19502" x2="19500" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            0</desc>
               <line stroke="blue" x1="19503" x2="19500" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="In a First-Class Cabin">
         <text font-size="2em" x="25500" y="50">In a First-Class Cabin</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            0</desc>
               <line stroke="red" x1="25501" x2="25500" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            0</desc>
               <line stroke="green" x1="25502" x2="25500" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            1</desc>
               <line stroke="blue" x1="25503" x2="25500" y1="0" y2="1"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="At the British Supreme Court">
         <text font-size="2em" x="26100" y="50">At the British Supreme Court</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            1</desc>
               <line stroke="red" x1="26101" x2="26100" y1="0" y2="1"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            0</desc>
               <line stroke="green" x1="26102" x2="26100" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            0</desc>
               <line stroke="blue" x1="26103" x2="26100" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Twisted Karma and His Last Bow">
         <text font-size="2em" x="26400" y="50">Twisted Karma and His Last Bow</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            0</desc>
               <line stroke="red" x1="26401" x2="26400" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            0</desc>
               <line stroke="green" x1="26402" x2="26400" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            1</desc>
               <line stroke="blue" x1="26403" x2="26400" y1="0" y2="1"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Turnabout for Tomorrow">
         <text font-size="2em" x="27000" y="50">Turnabout for Tomorrow</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            0</desc>
               <line stroke="red" x1="27001" x2="27000" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            1</desc>
               <line stroke="green" x1="27002" x2="27000" y1="0" y2="1"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            0</desc>
               <line stroke="blue" x1="27003" x2="27000" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="The Story's End">
         <text font-size="2em" x="28800" y="50">The Story's End</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            0</desc>
               <line stroke="red" x1="28801" x2="28800" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            1</desc>
               <line stroke="green" x1="28802" x2="28800" y1="0" y2="1"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            0</desc>
               <line stroke="blue" x1="28803" x2="28800" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Turnabout for Tomorrow">
         <text font-size="2em" x="30600" y="50">Turnabout for Tomorrow</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            0</desc>
               <line stroke="red" x1="30601" x2="30600" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            0</desc>
               <line stroke="green" x1="30602" x2="30600" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            1</desc>
               <line stroke="blue" x1="30603" x2="30600" y1="0" y2="1"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Turnabout Revolution">
         <text font-size="2em" x="32700" y="50">Turnabout Revolution</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            0</desc>
               <line stroke="red" x1="32701" x2="32700" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            1</desc>
               <line stroke="green" x1="32702" x2="32700" y1="0" y2="1"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            0</desc>
               <line stroke="blue" x1="32703" x2="32700" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="The Rite of Turnabout">
         <text font-size="2em" x="38100" y="50">The Rite of Turnabout</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            0</desc>
               <line stroke="red" x1="38101" x2="38100" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            0</desc>
               <line stroke="green" x1="38102" x2="38100" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            1</desc>
               <line stroke="blue" x1="38103" x2="38100" y1="0" y2="1"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Turnabout Reclaimed">
         <text font-size="2em" x="43200" y="50">Turnabout Reclaimed</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            1</desc>
               <line stroke="red" x1="43201" x2="43200" y1="0" y2="1"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            0</desc>
               <line stroke="green" x1="43202" x2="43200" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            0</desc>
               <line stroke="blue" x1="43203" x2="43200" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Mysterious Labyrinthia">
         <text font-size="2em" x="43800" y="50">Mysterious Labyrinthia</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            0</desc>
               <line stroke="red" x1="43801" x2="43800" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            1</desc>
               <line stroke="green" x1="43802" x2="43800" y1="0" y2="1"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            0</desc>
               <line stroke="blue" x1="43803" x2="43800" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
      <g id="Secrets of the Underground Ruins">
         <text font-size="2em" x="44400" y="50">Secrets of the Underground Ruins</text>
         <g class="cutinInfo"><!--ebb: We only have three cutIn lines, so let's plot these for each episode that contains them.
       We could try a stacked bar plot, or just plot them side by side.
       -->
            <g>
               <desc>Objection!, 
            0</desc>
               <line stroke="red" x1="44401" x2="44400" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Take that!, 
            1</desc>
               <line stroke="green" x1="44402" x2="44400" y1="0" y2="1"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
            <g>
               <desc>Hold it!, 
            0</desc>
               <line stroke="blue" x1="44403" x2="44400" y1="0" y2="0"/>
   `        <!--  ebb: Notice how I'm just adding the $cutPos value to the regular x value for the episode here. 
               We can do something different to try to stack these bars on top of each other instead
               of plotting them side by side
               
               WHERE TO PUT THE TEXT OF THE CUT-INS RELATIVE TO THESE THREE LINES?
                <text font-size="3em" x="{$xSpacer * $pos}" y="50">{$c}</text> --></g>
         </g>
      </g>
   </g>
</svg>
